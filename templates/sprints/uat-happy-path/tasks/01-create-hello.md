# Task 01: Create Hello File

## Context
This is a simple sanity-check task to verify the agent can create files.

## Requirements
- Create a file called `uat-output/hello.txt` in the project root.
- The file must contain exactly: `Hello from Clifford!`
- Create the `uat-output/` directory if it does not exist.

## Verification
1. Confirm `uat-output/hello.txt` exists.
2. Confirm the content is `Hello from Clifford!`.
3. Mark this task as `completed` in the manifest.
